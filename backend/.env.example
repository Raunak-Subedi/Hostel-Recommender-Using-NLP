# Copy this file to `.env` and adjust as needed.
# By default the app runs with zero setup using SQLite (see config.py).

# Uncomment and edit to use MySQL via XAMPP/WAMP instead (as described in the proposal):
# DATABASE_URL=mysql+pymysql://root:@localhost:3306/hostel_recommender

SECRET_KEY=change-this-to-a-random-secret-in-production
FLASK_DEBUG=1
